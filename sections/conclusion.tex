% =====================================================================
% Section 7: Conclusion
% =====================================================================

\section{Conclusion}
\label{sec:conclusion}

In a big world there is no examiner outside the agent, so the regret that matters is the one a bounded internal examiner can certify. By Proposition~\ref{prop:nothing} the self-report is empty without soundness; by Lemma~\ref{lem:bridge} soundness makes it a certificate; by Proposition~\ref{prop:bandit} the certificate recovers the classical theory when the agent can cover its world; and by Theorem~\ref{thm:nofree} it cannot be driven to zero where the comparator is non-identifiable from the stream. Meeting soundness forces the examiner to out-learn the policy it grades. The position turns evaluation in CRL into a budgeting problem between acting and judging, and makes ``is the agent still learning?'' the operational question answered by a sound, shrinking $\rho_t$.
